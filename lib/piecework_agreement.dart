import 'package:flutter/material.dart';

class PieceworkAgreement extends StatelessWidget {
  const PieceworkAgreement({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: Text('Piecework Agreement')),
    body: Center(child: Text('Piecework Agreement Screen')),
  );
}
