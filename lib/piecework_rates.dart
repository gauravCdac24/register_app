import 'package:flutter/material.dart';

class PieceworkRates extends StatelessWidget {
  const PieceworkRates({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: Text('PieceWork Rates')),
    body: Center(child: Text('PieceWork Rates Screen')),
  );
}
